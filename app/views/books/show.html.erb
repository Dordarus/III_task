<% provide(:title, @book.title) %>
<div class="container">
    <div class="page-header">
        <h1><strong><%= @book.title %></strong></h1>      
    </div>
    <h3><i><%= @book.genre %></i></h3>
    <h4><%= @book.year %></h4>
    <p><i><%= @book.plot %></i></p>  
    <hr>
    <%= render "shared/user's_book_options"%>
</div>